# Example parameters for fix-system-inconsistencies-v2 workflow

# Standard execution (applies all fixes)
SYSTEM_ROOT: ".claude"
DOCS_DIR: ".claude/docs"
SCHEMAS_DIR: ".claude/schemas"
AGENTS_DIR: ".claude/agents/workflow"
COMMANDS_DIR: ".claude/commands"
TEMPLATES_DIR: ".claude/templates"
DRY_RUN: false

# ---
# Dry run configuration (report only, no changes)
# Uncomment below and comment above to use:
# ---
# SYSTEM_ROOT: ".claude"
# DOCS_DIR: ".claude/docs"
# SCHEMAS_DIR: ".claude/schemas"
# AGENTS_DIR: ".claude/agents/workflow"
# COMMANDS_DIR: ".claude/commands"
# TEMPLATES_DIR: ".claude/templates"
# DRY_RUN: true
