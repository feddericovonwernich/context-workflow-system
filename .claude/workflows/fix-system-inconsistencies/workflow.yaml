name: fix-system-inconsistencies
description: "Fix all identified inconsistencies in the context workflow system documentation, schemas, and prompts"
version: 1.0.0

parameters:
  - name: BASE_DIR
    type: directory
    required: false
    default: ".claude"
    description: "Base directory containing the workflow system files"

phases:
  require_confirmation: false
  allow_retry: true
  generate_logs: true
  stop_on_failure: true

metadata:
  generated_from:
    - "Technical inconsistency analysis"
  generated_date: "2025-01-13"
  workflow_type: automation
  complexity: medium
  architecture_notes:
    - "Ultrathin phases - minimal overhead, maximum coverage"
    - "Each phase is independently verifiable"
    - "Fixes are atomic and can be reviewed individually"
