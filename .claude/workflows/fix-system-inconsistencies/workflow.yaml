name: fix-system-inconsistencies
description: "Comprehensive workflow to fix all 27 identified inconsistencies in the Context Workflow System documentation, schemas, agents, and commands"
version: 1.0.0

parameters:
  PROJECT_ROOT:
    type: directory
    required: true
    description: "Root directory of the context-workflow-system project"
    default: "/home/fedderico/Repos/agents/context-workflow-system"

  BACKUP_ENABLED:
    type: boolean
    required: false
    default: true
    description: "Create backups before modifying files"

  STRICT_MODE:
    type: boolean
    required: false
    default: true
    description: "Fail on any validation error after fixes"

  DRY_RUN:
    type: boolean
    required: false
    default: false
    description: "Show changes without applying them"

phases:
  require_confirmation: true
  allow_retry: true
  generate_logs: true
  stop_on_failure: true
  parallel_execution_supported: false

metadata:
  generated_from:
    - "Technical analysis of system inconsistencies"
  generated_date: "2025-01-13"
  workflow_type: migration
  complexity: complex
  supported_agents:
    - phase-executor
  architecture_notes:
    - "Fixes must maintain backward compatibility"
    - "Schema changes require documentation updates"
    - "All 27 findings must be addressed"
    - "Changes must not introduce new inconsistencies"
